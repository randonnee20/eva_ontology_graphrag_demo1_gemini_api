# =====================================================
# eva_ontology_graphrag_demo1 - 하이브리드 LLM 버전
# Python 3.12
# =====================================================

# Core
streamlit>=1.32.0
pyyaml>=6.0

# 환경변수 로드 (로컬 개발용)
python-dotenv>=1.0.0

# ──────────────────────────────────────────────────
# LLM - 로컬 llama-cpp (로컬 PC 전용)
# Render 배포 시 주석처리 권장 (빌드 시간 과다)
# 로컬 설치: pip install llama-cpp-python --break-system-packages
# ──────────────────────────────────────────────────
# llama-cpp-python>=0.3.0

# LLM - Gemini API (Render fallback)
google-genai>=1.0.0

# Vector DB
faiss-cpu>=1.7.4
sentence-transformers>=2.6.0

# Graph
networkx>=3.2
neo4j>=5.18.0

# Document parsing
python-docx>=1.1.0
pypdf>=4.1.0
olefile>=0.47

# Graph visualization
pyvis>=0.3.2

# ML
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
numpy>=1.26.0

# Utils
tqdm>=4.66.0